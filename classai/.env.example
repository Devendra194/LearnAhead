# The Android app does not contain AI provider credentials.
# The default debug URL is http://10.0.2.2:8000/ for the Android emulator.
# Override it at build time for a physical device or hosted API:
#   gradle assembleDebug -PCLASSAI_API_BASE_URL=https://api.example.com/
